{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "version": "0.1.3",
    "packageManager": "yarn@4.5.3",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "test": "turbo run test"
    },
    "devDependencies": {
        "turbo": "^2.4.0",
        "typescript": "^5.7.3"
    }
}
